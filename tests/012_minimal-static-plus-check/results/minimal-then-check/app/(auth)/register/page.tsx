import { RegisterForm } from "@/features/auth/components/register-form";

export const metadata = { title: "Create Account - TaskFlow" };

export default function RegisterPage() {
  return <RegisterForm />;
}
